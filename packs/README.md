# Tables

Critical and attack tables are shipped as HTML under `data/critical/` and `data/attacks/`.
The Critical Tables browser (scene control, Alt+C, or sheet window control) loads them at runtime.

This avoids shipping a LevelDB journal pack while still giving one-click access at the table.
